{
  "name": "toy-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode ssr",
    "build": "qwik build",
    "build.client": "vite build",
    "build.server": "vite build -c adapters/static/vite.config.ts",
    "preview": "npx http-server dist",
    "ssg": "pnpm run build && pnpm run build.server && pnpm run preview"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.17.2",
    "@thi.ng/equiv": "^2.1.99",
    "ignore": "^7.0.5"
  },
  "devDependencies": {
    "@builder.io/qwik-city": "^1.17.2",
    "@types/node": "^20.8.4",
    "typescript": "5.4.5",
    "vite": "7.1.11",
    "vite-tsconfig-paths": "^4.2.1"
  }
}
